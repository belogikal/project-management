.container-fluid.px-3
  .row.align-items-center.mb-2
    .col-8
      %h1.mb-0 Projects
    .col-4.text-end
      %a.btn.btn-sm.btn-primary{href: new_project_path} New Project

- @resources.each do |resource|
  .card.mb-2
    .card-body
      %h5.card-title
        = link_to resource.name, resource
      %div.text-right.pt-3
        = link_to "Edit", edit_project_path(resource), class: "btn btn-sm btn-secondary mr-2"
        = button_to "Delete", project_path(:id => resource.id), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-sm btn-danger", style: "padding: 0.375rem 0.75rem; background-color: #dc3545; border-color: #dc3545;"
